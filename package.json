{
  "name": "empresa-base",
  "version": "1.0.0",
  "private": true,
  "description": "Ambiente de referencia (dev/QA/prod) del modelo Organization-base",
  "scripts": {
    "test": "playwright test",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
